# Task Manager — Factory Upgrade (Canonical)
# Format: TOON — managed via manage.sh
# Usage:  .factory/task-manager/manage.sh list|add|start|complete

summary:
  completed: 39
  next: 5
  in_progress: 0
  cancelled: 5

  completed:
    - us_074_daemon_ui: "DaemonStatus.tsx (start/stop/restart, live status)"
    - us_062_project_init_ui: "ProjectInitWizard.tsx (2-step modal, stack detection)"
    - us_053_ui_engine_badge: "EngineBadge.tsx (factory=blue, minions=purple)"
    - us_051_052_ui_context_skills: "ToonContextViewer.tsx, SkillRegistry.tsx (API routes)"
    - us_050_ui_heartbeat: "HeartbeatMonitor.tsx (30s poll, status dots)"
    - us_014_build_tool_ui: "BuildTimeline.tsx (SSE stream from /api/build-events)"
    - us_073_spec_watcher: "Added factory queue watch <dir> — auto-enqueue new YAML specs, persistent file watcher"
    - us_072_daemon_process_mgr: "Added factory daemon start/stop/status/restart — PID file management, detached spawn"
    - us_071_auto_retry: "Auto-retry up to 3x with exponential backoff (5s, 10s, 20s)"
    - us_070_queue_daemon: "Added startQueueDaemon() — persistent watch loop, 30s poll, SIGTERM/SIGINT handling"
    - us_040_043_workflows_hooks: "Created factory/scripts/heartbeat/setup-hooks.sh — pre-commit + post-commit"
    - us_024_human_toon_input: "Added --preserve-human flag in manage.sh (skip slugification for TOON markers)"
    - us_022_write_tasks_toon: "writeTasksToon() in writer.ts — updates todo.toon after knowledge entry"
    - us_033_034_engine_field: "engine field (factory|minions) integrated across types, spec, db, cli"
    - us_005_write_factory_yaml: "factory.yaml already created by us_002 — stale"
    - us_006_task_summary_qa: "todo.toon summary validation — stale (manage.sh handles counts)"
    - us_099_final_integration: "Final validation: tsc --noEmit passes clean. All P0/P1 tasks implemented."
    - us_061_agents_md_patch: "Added to initBridge: agents.md template + Factory Agentic Scaffold section"
    - us_060_init_bridge_engine: "engine/init.ts with initBridge(repoPath) — full .factory/ scaffold + detectStack()"
    - us_031_032_minions_engine: "engine/minions-engine.ts — specToQueueYaml, runMinionsEngine, runMinionsBuild"
    - us_030_delete_old_engines: "Deleted gemini-cli-engine.ts and pi-cli-engine.ts. BuildEngine=factory|minions"
    - us_023_heartbeat_on_queue: "writeHeartbeat() in markRunning() with try/catch"
    - us_021_gather_toon_context: "gatherToonSnapshot() in context.ts, toonSnapshot/projectSkills in ProjectContext"
    - us_020_toon_helpers: "engine/toon.ts — readToonFile, compressToon, parseToonSkillIndex, writeHeartbeat, writeToonTasksSnapshot"
    - us_013_tool_system_prompt: "buildToolSystemPrompt() — role, RULES, REASON CODES, TOON context, skills"
    - us_012_tool_calling_loop: "runToolSession() — max 30 turns, token guard at 20 msgs, stops on ctx.terminal"
    - us_011_tool_executor: "executeTool() — async, never throws, errors as {content, isError}"
    - us_010_build_tool_schema: "engine/build-tools.ts — 9 TOOL_DEFINITIONS in OpenAI function-call format"
    - us_004_cli_facade: "CLI facade subcommands: pulse, task, context, compress, minions, hooks"
    - us_003_factory_yaml_schema: "factory_home/agentic in BridgeConfig, resolveScript() in cli.ts"
    - us_002_factory_scaffold: "factory/.factory/ self-referential bridge scaffold"
    - us_001_copy_scripts: "Copied 6 skill scripts, macOS UTC fix, @toon-format/toon dep"
    - us_049_task_manager_mv: "Moved task-manager manage.sh scripts into .factory/task-manager/ folder, updated path references." [done]
    - us_050_skill_index_toon: "Created .factory/skill-index.toon with 6 skill entries for the Factory project itself." [done]
    - us_080_factory_self_factory_yaml: "Wrote /Users/pretheesh/Projects/factory/.factory/factory.yaml with absolute factory_home and .yaml file references." [done]
    - us_081_remove_stale_api_routes: "Deleted /api/settings/gemini-cli-check/ and /api/settings/pi-cli-check/ route directories." [done]
    - us_082_extract_daemon_ts: "Confirmed daemon logic stays in queue.ts (no separate daemon.ts needed); CLI spawn uses queue.ts directly." [done]
    - us_083_wire_initbridge_to_add: "addProject() in config.ts now calls initBridge() instead of ensureFactoryDir(). Full scaffold created on CLI project add." [done]
    - us_084_copy_manage_sh: "initBridge() now copies manage.sh from factory root into .factory/task-manager/manage.sh with chmod +x." [done]
    - us_085_implement_agents_md_patch: "Implemented patchAgentsMd() in init.ts — creates AGENTS.md if missing, appends Factory section if existing file lacks it." [done]
    - us_086_analyze_existing_codebase: "Implemented analyzeExistingProject() in init.ts — reads README, package.json, file tree, detects conventions, writes rich context.yaml." [done]
    - us_087_toon_format_files: "All initBridge() files now written as YAML using yaml.stringify(). No more JSON.stringify() in .toon files." [done]
    - us_088_skip_existing_files: "initBridge() checks existsSync() before writing — existing context.yaml, heartbeat.yaml, worklog.yaml, skill-index.yaml, todo.yaml are skipped." [done]
    - us_089_fix_workflow_source_path: "Workflow source path now uses getFactoryRoot() based on import.meta.url, not process.cwd()." [done]
    - us_090_factory_home_absolute: "factory_home in initBridge() is now absolute path from getFactoryRoot(). skill-index.yaml uses absolute paths for all factory scripts." [done]
    - us_095_toon_as_codec_not_storage: "TOON is now codec-only. Storage files renamed to .yaml. gatherToonSnapshot() reads YAML, calls encode() at injection time. toon.ts writes heartbeat.yaml and tasks.yaml. pulse.sh writes heartbeat.yaml." [done]

  next:
    - us_091_init_sse_stream: "/api/project/init returns a plain JSON response after initBridge() completes. Upgrade to SSE: write each file creation as a text/event-stream event with {file, action} payload. Client-side: switch from fetch+json to EventSource or fetch+ReadableStream. Show checkmarks animating in real time." [P2 ui/src/app/api/project/init/ ui/src/components/ProjectInitWizard.tsx]
    - us_092_failure_knowledge: "On build failure, markFailed() records in SQLite but writes nothing to disk. Fix: in queue.ts markFailed(), write .factory/knowledge/failures/{slug}-{timestamp}.md with: spec slug, error message, attempt count, timestamp, tsc output if available. Future builds read this via gatherContext() to avoid repeating the same mistake." [P2 engine/queue.ts engine/context.ts]
    - us_093_launchd_plist: "factory daemon install is referenced in plan but not implemented. Implement: generate a macOS launchd plist at ~/Library/LaunchAgents/com.factory.daemon.plist that runs 'factory daemon' on login. Write plist with correct WorkingDirectory and EnvironmentVariables. Run: launchctl load to activate. factory daemon uninstall removes it." [P2 engine/cli.ts]
    - us_094_cli_hooks_on_add: "factory project add does not trigger git hooks installation. After initBridge() is wired (us_083), also run setup-hooks.sh in the target repo if it is a git repo (check for .git/). Prompt user: 'Install git hooks? (y/n)' in CLI. In UI wizard: the hooks toggle already exists — wire it to call factory hooks install after init completes." [P2 engine/config.ts engine/cli.ts]

  in_progress:

  cancelled:
    - us_005_write_factory_yaml: "Stale — factory.yaml already created by us_002"
    - us_006_task_summary_qa: "Stale — todo.toon summary already handled by manage.sh"
    - us_022_write_tasks_toon: "Stale — writeTasksToon already implemented in writer.ts"
    - us_024_human_toon_input: "Stale — --preserve-human flag already added to manage.sh"
    - us_033_034_engine_field: "Stale — engine field already integrated across types, spec, db, cli"
