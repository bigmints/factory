heartbeat:
  last_seen: "2026-05-30T14:12:23Z"
  host: "Mac"
  task: "post-commit: fix(rollup): roadmap now shows all physical stories, not just scaffold.yaml entries

getAppRollup now scans features/, apps/, done/ for story files not yet
tracked in scaffold.yaml and injects them as in-memory synthetic epics
before rollup computation. Read-only — no disk writes, no queue impact.

This makes the Roadmap view match the Sprint board: all stories appear
regardless of whether they were added via scaffold.yaml or directly as
physical YAML files."
  status: alive
