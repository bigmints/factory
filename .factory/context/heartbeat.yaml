heartbeat:
  last_seen: "2026-05-30T14:06:33Z"
  host: "Mac"
  task: "post-commit: fix(orchestrate): add ~/.local/bin to CLI spawn PATH so gemini is found

gemini CLI is installed at ~/.local/bin/gemini but that directory was
missing from the PATH injected when spawning CLI child processes, causing
ENOENT. Added ~/.local/bin (and /usr/local/bin) ahead of nvm bin paths
so all common install locations are covered."
  status: alive
