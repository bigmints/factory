heartbeat:
  last_seen: "2026-06-08T21:18:22Z"
  host: "BigMac-2"
  task: "post-commit: fix(tpm): fix all 4 audit gaps in TPM chat route

- list_stories: recurse subdirectories with collectYamlFiles()
  (was missing features in subdirs like pi-app's features/antigravity/)
- get_project_status + read_heartbeat: read blueprint/worklog.yaml
  (TOON format) instead of logs/worklog.yaml so agent session logs
  are actually visible to the TPM
- decompose_requirements: inject story-generator SKILL.md + full
  userStory/Given-When-Then acceptanceCriteria schema into prompt
- TPM system prompt: inject skill-index.yaml at request time so the
  LLM knows which project skills are available"
  status: alive
